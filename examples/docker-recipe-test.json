{
  "name": "docker-recipe-test",
  "description": "Test pipeline demonstrating Docker recipe with new interface",
  "steps": [
    {
      "id": "setup",
      "name": "Setup environment",
      "action": {
        "type": "shell",
        "command": "echo 'Setting up environment'"
      }
    },
    {
      "id": "docker-test",
      "name": "Run Docker container",
      "depends_on": ["setup"],
      "action": {
        "type": "docker",
        "parameters": {
          "image": "alpine:latest",
          "command": "echo 'Hello from Docker!'"
        }
      }
    }
  ]
}
